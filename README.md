# Business Investment Dashboard — offline PWA

Deployed as a static site with a localStorage-based storage shim,
a manifest for installability, and a service worker for offline caching.

## Update later
If you edit index.html, bump CACHE_NAME in sw.js (v1 -> v2) so
installed copies pick up the change.
